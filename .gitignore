node_modules/
vibe-source/
dist/
.env
.env.*
*.local

# PAC CLI auth cache
.pac/

# Power Apps local run cache
.power/

# OS
.DS_Store
Thumbs.db

# VS Code
.vscode/settings.json

# Playwright browser binaries (installed via npx playwright install)
vibe-extractor/node_modules/

# Generated project output (user creates their own)
# Uncomment if you want to exclude generated projects too:
# [A-Z]*/dist/
# [A-Z]*/node_modules/

OMS/

# CRITICAL: saved Microsoft account tokens for vibe-verifier — never commit
vibe-verifier/.auth/

# Claude Code local settings (per-machine, not shared)
.claude/settings.local.json

# Toolkit-on-toolkit artifacts: these only appear if vibe-extractor / vibe-reporter
# is run inside the toolkit repo by accident. In a real user project these
# files SHOULD be committed — this rule only protects the toolkit repo itself.
vibe-history.json
vibe-pending-update.json
vibe-baseline/
vibe-baselines-snapshots/
vibe-migration.json
vibe-lock
tests/results/
tests/specs/

# Playwright artifacts written during smoke runs
playwright-report/
test-results/
